@model IEnumerable<CoverType> 
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    
}

@* i displayed the temporary message here because after every crud operation program redirects the user to this page.   *@
<div class="container p-3">
    <div class="row pt-4">
        <div class ="col-6">
            <h2 class="text-primary">CoverType Controller</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn-primary btn">Create New</a>
        </div>
    </div>
    <br />  
    
    <table class = "table table-bordered table-striped">
        <thead>
            <tr>
                <th>Cover Type Name</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
           {
            <tr>

                <td width="70%">
                    @item.Name
                </td>
                <td>
                    <a asp-controller="CoverType" asp-action="Edit" asp-route-id=@item.Id class ="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i> Edit</a>
                    <a asp-controller="CoverType" asp-action="Delete" asp-route-id=@item.Id class ="btn btn-danger mx-2"><i class="bi bi-trash"></i> Delete</a>
                </td>
            </tr>   
           }
        </tbody>
    </table>
</div>
